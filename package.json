{
  "name": "copipe-haiku",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "build:css": "tailwindcss -i ./styles/input.css -o ./dist/styles.css --minify",
    "build:js": "node ./scripts/build-offline.mjs",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
